1. Install `ollama`: `curl -fsSL https://ollama.com/install.sh | sh`
2. Run `ollama`: `ollama` in the terminal.
3. Run the `qwen3` model: `ollama run qwen3`.
4. Run `uv run weather-llm` to run the project.
